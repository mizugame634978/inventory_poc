# #0012 memory スナップショットスクリプト(後回し / backlog)

- status: open  ← 未着手。今すぐやらない。やりたくなったらこの issue から着手する。
- blocked: 人間の判断(出力先 A/B)待ち。自走ループの対象外。
- created: 2026-06-21

## 目的 / 背景
個人開発者向け方針(#0011)で memory は有効。可搬性(別マシンへ持ち運ぶ)と可視化(何を学んだか
人が見る)のために、ホームの memory を**スナップショット**としてリポジトリ側へコピーする
スクリプトが欲しい。ただし急ぎではないため後回し。

## やること(着手時)
- `scripts/snapshot-memory.ps1`:
  - **cwd からプロジェクトの memory キーを導出**する(特定環境の絶対パスをハードコードしない=パス規約)。
    例: 現在の作業ルートを Claude Code のサニタイズ規則(`:` と `\` `/` を `-`)でキー化し、
    `~/.claude/projects/<キー>/memory/` を特定する。
  - その配下の `*.md` をリポジトリ内の出力先へコピーする。

## 設計判断(着手時に決める / 未決)
- 出力先と git 扱い:
  - 案A: `docs/agent-memory-snapshot/` に置き **コミット**(可搬・共有されるが実質ドキュメント化)。
  - 案B: リポジトリ内だが **gitignore**(可視化・バックアップ用、共有しない)。← 個人用途なら無難
  - → 着手時に A/B を選ぶ。
- 逆向き(リポジトリ → ホーム memory への復元)も要るか。

## 完了条件(着手時)
- スクリプトが環境非依存に動く(絶対パス無し、cwd 起点)
- スナップショットが出力先に作られる
- README/CLAUDE.md に使い方を1行追記

## 結果(完了時に追記)
- done:
- 要点:
- commits:
